# 對抗式審查報告 — `docs/spec.md`（Palladio Design Language & System）

> 審查日期：2026-09-13
> 審查方式：`maze-adversarial-review`，由未參與規格形成、乾淨上下文的獨立 sub-agent 執行
> 範圍：`docs/spec.md` 第 1–11 章；`DECISIONS.md`、`NEXT_ACTION.md`、`AGENTS.md`、可及性契約、git/Issue/PR 歷史作為佐證

## 一、審查目標

嘗試證偽 `docs/spec.md` 第 1–11 章目前主張的方向是否站得住腳，重點檢驗：規格中被 `DECISIONS.md` 標記「有效」的決策，究竟有獨立證據／使用者裁決支持，還是只是 agent 寫下來後被當成既定事實層層疊加的斷言。

## 二、Reviewer 獨立性

未參與規格形成過程，僅依 repo 內文件、`git log`、`gh issue/pr` 唯讀查詢建立判斷；未修改任何檔案。

## 三、核心主張、隱藏假設與可推翻條件

| # | 核心主張 | 出處 | 隱藏假設 | 可推翻條件 |
|---|---|---|---|---|
| A | 第一至八章設計語言是「source of truth」，六個既有元件已完成且品質達標 | `docs/spec.md` 第 1–8 章；`NEXT_ACTION.md:7` | 元件實作出來的視覺結果，經得起真人視覺品味檢驗 | 使用者本人明確表示不滿意，且診斷出「從未有人用視覺品味設計過」 |
| B | 第十一章「驗證策略」（生產環境接入取代合成壓力測試）比原本的壓力測試更嚴謹，已在運作 | `docs/spec.md` 第十一章；`DECISIONS.md:16` 標「有效」 | 現實中確實有真實產品或網頁已接入並產生回饋 | `main` 上唯一候選「真實網頁」根本沒有消費 token 套件 |
| C | Accent 六插槽開放＋「不強迫所有產品長一樣」是可實現的架構承諾 | `docs/spec.md:37-39, 96-107` | Layer 2 元件級 override 與多元產品差異化是真實存在或至少可行的機制 | Layer 2 從未被實作、且從無第二個獨立產品驗證過「長不一樣」 |
| D | 可及性 A-M1/A-M2 的 `[MUST]` 硬規則已用自動化 gate 保證不回歸 | `accessibility-contract.md` 第三節 | 通過門檻的數值有合理安全邊際 | 某 token 通過但安全邊際趨近於 0 |
| E | Issue #4／#8／#14／#15 的關閉代表相應驗收條件已被滿足 | `NEXT_ACTION.md:7`；commit `420c579` | 關閉是因為條件達成，而非條件被重新定義以繞過未達成的部分 | 關閉理由是「改寫規格降低門檻」而非「達成原門檻」 |

## 四、已執行的攻擊與查核結果

### 攻擊 1 — 主張 A：六元件是否真的「完成且品質達標」

- `DESIGN_REVIEW-2026-09-11.md`（Codex `maze-design-review`，2026-09-11）對六元件評分：總分 8.75，「高優先問題：無」。
- 僅隔一天，`gh issue view 84`（2026-09-12）記錄：使用者對六元件外觀**極其不滿意**，點名側邊導覽選單方正難看；根因是 §4.1 圓角表從未列 nav item/tab/menu item，agent 只能自己發明或乾脆不加。
- `gh issue view 85`（同日）更根本：「**從來沒有人以視覺品味實際設計過它們**——agent 在 spec 的真空裡自己填，拿去給另一個 agent 審查，得到的結論永遠只會是『它沒被設計過』，是拿沒有答案的考卷對答案」。使用者已決定親自畫五種狀態的視覺稿，作為未來規格「第十二章」的原始素材。

**判定：主張 A 被推翻。** Agent 美學審查給出的高分不是使用者實際視覺滿意度的有效代理指標，且非孤例（Issue #74 背景記錄過同一模式：數字合規、質感不合格）。

### 攻擊 2 — 主張 B：第十一章「生產環境驗證」是否真的在運作

- Commit `420c579`（使用者本人帳號 `tigerzhang`，Co-Authored-By Claude）把第十一章從「壓力測試」改寫為「驗證策略」，commit message 自陳：Issue #4/#8/#14/#15 依此策略關閉（合成壓力測試＋兩產品 accent 驗證不再是完成 gate）。
- 但改寫發生在驗收條件明確**尚未達成**之後：PR #32 自身摘要寫明「不滿足 #15（需要兩個真實產品的實際 accent 色值）；本原型 accent 為檢視素材用虛構資料」，PR #32/#33 最終皆 **CLOSED（未合併）**。
- 檢查 `main` 上唯一被合併、宣稱代表「真實產品接入」的候選（`site/index.html` + `site/assets/css/styles.css`，PR #47/#48）：
  - `styles.css` 手動複製 Palladio 表面色為自己的變數，並明文註解 `--brand` **「NOT a Palladio token」**。
  - 全檔 `grep -c "var(--pd-"` = **0**——沒有任何一處實際引用 `@pdiodsgn/tokens`。
  - 真正會 import 實際 token/元件 CSS 的文件站建置只存在於 **PR #70（OPEN，未合併）**，而 `NEXT_ACTION.md:12` 明確記錄「使用者已明確要求先不要合併」。

**判定：主張 B 被推翻（就 `main` 分支現況而言）。** 截至審查時點，`main` 上沒有任何真實產品實際消費 Palladio token 套件；唯一候選是手動抄色值、明確聲明「不是 Palladio token」的靜態頁面。規格與 `DECISIONS.md:16` 卻已把「生產環境驗證」列為「有效」的既定策略。

### 攻擊 3 — 主張 C：「不強迫所有產品長一樣」是否有結構性支持

- `docs/spec.md` §9.1/§9.5 承諾 Layer 2「元件級 override，各產品可選擇使用」。
- 實查：`palladio/tokens/component/` 目錄**不存在**；六個已出貨元件 CSS 中比對 `pd-button-*`／`pd-card-*`，命中的全是 CSS class 名稱，沒有任何 Layer 2 custom-property token 被實際定義或消費過。

**判定：屬於「無法判定 / insufficient evidence」，而非證偽。** 不是錯的，但目前是完全未被實踐檢驗的架構承諾，卻已寫入規格並與其他「有效」決策並列。

### 攻擊 4 — 主張 D：可及性 MUST 規則的安全邊際

- `border-strong`／`input-border` 對 `surface-overlay` 的對比為 **3.03:1**，門檻 **3:1**——安全邊際僅 0.03。PR #81/commit `3ccb364` 自己承認「餘裕只有 0.03，接近門檻，值得記錄關注」。

**判定：部分成立、部分需警示。** Gate 機制本身真實可重現，但這條 MUST 規則貼著臨界值運作，任何後續對相關 token 的四捨五入級調整，都可能無聲跌破門檻——而它恰好是五個元件 focus ring 的最後防線。

### 攻擊 5 — 主張 E：Issue 關閉是否代表達成，而非重新定義達成

已於攻擊 2 詳述：PR #32 明文承認未滿足 #15，PR #32/#33 均未合併，隨後規格被改寫，四個 issue 依新規格關閉。額外查核 `SPEC_REVIEW.md`：SR-005 狀態仍是 `open`，但 `accessibility-contract.md` 已單方面把它「解決」，兩份文件狀態記錄不一致。

**判定：主張 E 被推翻。** 至少四個 issue 的關閉，是透過重新定義「完成」標準達成，而非滿足原始標準。

## 五、公平的替代解釋

1. **主張 A／B**：這可能是年輕開源設計系統從「合成驗證」轉向「真實產品迭代」的正常早期陣痛。Issue #84/#85 的出現，恰恰證明「使用者親自監督、發現問題就開工單」這個機制**正在運作**，是自我修正能力的證據，不是規格失敗的證據。commit `420c579` 是使用者本人 commit，代表這是經人類同意的方向調整，不完全等同 agent 單方面灌水。
2. **主張 C**：Layer 2 未實作可能只是「還沒有需求出現」（規格本就標注「可選」），不代表機制不可行；三層架構的紀律面（Primitive 不得直接用於元件）經查核是被遵守的。

這些替代解釋能部分緩和證偽的嚴重程度，但**不能推翻**已發現的具體落差：無論改寫是否經人類同意，`main` 上「生產環境驗證」機制客觀上尚未真正運作過一次；`DECISIONS.md`／`NEXT_ACTION.md` 呈現的「已驗證」語氣與此不符。

## 六、殘餘不確定性

- 無法取得 `docs/taylor-agent-v1-spec.md`（不在本 repo 內），無法判斷「TaylorAgent」是否為真正獨立於使用者之外的第三方消費端。
- `@pdiodsgn/tokens` 是否有本 repo 之外的下游採用者，未查（超出唯讀 repo 稽核範圍）。
- Issue #84/#85 觸發的「第十二章」重建工作尚未開始，最終品質無法預先判定。

## 七、結論：`revise`

- 第 1–8 章的**技術性**主張（三層 token 架構紀律、WCAG 對比數學、semantic-only、Reduced Motion 契約）抽樣查核站得住腳，不建議整體推翻或停止。
- 但至少三項被當作「已達成／有效」的關鍵狀態主張經查證**不成立或缺乏證據**：
  1. 六元件視覺品質已完成——已被使用者本人於 Issue #84/#85 推翻；
  2. 第十一章「生產環境驗證」策略——`main` 上尚無任何真實消費端運作過，唯一合併的「官網」明確聲明不使用 Palladio token；
  3. Issue #4/#8/#14/#15 的關閉——靠重新定義完成標準達成，而非滿足原始驗收條件。
- 額外風險（需監控非立即修正）：A-M2 對 `border-strong`/`input-border` 在 `surface-overlay` 上的安全邊際僅 0.03，貼著 MUST 規則臨界值。
- 使用者已經在用 Issue #84/#85 自我修正主張 A，是健康訊號，不需額外介入；但 `DECISIONS.md`／`NEXT_ACTION.md` 對主張 B／E 的狀態描述應被修正為誠實反映現況（例如把「驗證策略以生產環境接入為主」的「有效」標記，改註記「機制已定義，尚無 main 分支上的真實案例」），而不是任由「規格說了就等於做到了」的落差被後續決策當成既定事實疊加。

**檔案／行號索引**：`docs/spec.md:37-39, 96-107, 274-330, 365-369`；`DECISIONS.md:16, 18-20`；`NEXT_ACTION.md:7, 12`；`accessibility-contract.md:60-71`；`DESIGN_REVIEW-2026-09-11.md:10-24`；`site/assets/css/styles.css:1-12`；commit `420c579`、`3ccb364`；PR #32、#33、#70；Issue #14、#15、#74、#84、#85。
